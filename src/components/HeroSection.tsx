import type { ReactNode } from "react";
export function HeroSection({ title, tagline, intro, rightSlot }: { title: string; tagline: string; intro?: string; rightSlot?: ReactNode }) {
  return <section className="theme-surface hero-gradient-motion relative overflow-hidden rounded-3xl border border-amber-400/20 p-6 md:p-10"><div className="pointer-events-none absolute -top-20 -left-20 h-64 w-64 rounded-full bg-amber-400/10 blur-3xl" /><div className="pointer-events-none absolute -right-20 -bottom-20 h-64 w-64 rounded-full bg-emerald-400/10 blur-3xl" /><div className="pointer-events-none absolute inset-0 opacity-20 [background:repeating-linear-gradient(-45deg,transparent,transparent_8px,rgba(216,177,102,0.06)_8px,rgba(216,177,102,0.06)_16px)]" /><div className="relative grid gap-6 md:grid-cols-[1fr_auto] md:items-end"><div><p className="text-xs tracking-[0.25em] text-amber-300 uppercase">Historical Memory Journey</p><h1 className="mt-3 text-4xl font-semibold tracking-tight md:text-6xl">{title}</h1><p className="mt-4 max-w-2xl text-xl text-amber-50/95 md:text-3xl">{tagline}</p>{intro ? <p className="theme-muted mt-4 max-w-3xl text-base md:text-lg">{intro}</p> : null}<p className="mt-8 text-xs tracking-[0.22em] text-amber-200/90 uppercase md:text-sm">Scroll ↓ to begin the journey</p></div>{rightSlot}</div></section>;
}

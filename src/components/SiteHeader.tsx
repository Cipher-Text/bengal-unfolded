import Link from "next/link";
import { LanguageSwitcher } from "@/components/LanguageSwitcher";
import { ThemeToggle } from "@/components/ThemeToggle";
import type { Locale } from "@/types/content";
export function SiteHeader({ locale, pathAfterLocale }: { locale: Locale; pathAfterLocale?: string }) {
  return <header className="theme-header sticky top-0 z-40 border-b border-amber-500/20 backdrop-blur"><div className="mx-auto flex w-full max-w-6xl items-center justify-between gap-3 px-4 py-3 md:px-8"><div className="flex items-center gap-3"><Link href={`/${locale}`} className="text-sm font-semibold tracking-[0.2em] text-accent uppercase">Bengal Unfolded</Link><Link href={`/${locale}/topics`} className="inline-flex min-h-[44px] items-center rounded-lg border border-amber-500/35 px-2.5 text-xs font-medium text-accent hover:bg-amber-500/10">{locale === "bn" ? "টপিকস" : "Topics"}</Link><Link href={`/${locale}/figures`} className="inline-flex min-h-[44px] items-center rounded-lg border border-amber-500/35 px-2.5 text-xs font-medium text-accent hover:bg-amber-500/10">{locale === "bn" ? "ব্যক্তিত্ব" : "Figures"}</Link><Link href={`/${locale}/resources`} className="inline-flex min-h-[44px] items-center rounded-lg border border-amber-500/35 px-2.5 text-xs font-medium text-accent hover:bg-amber-500/10">{locale === "bn" ? "রিসোর্স" : "Resources"}</Link><Link href={`/${locale}/glossary`} className="inline-flex min-h-[44px] items-center rounded-lg border border-amber-500/35 px-2.5 text-xs font-medium text-accent hover:bg-amber-500/10">{locale === "bn" ? "গ্লসারি" : "Glossary"}</Link></div><div className="flex items-center gap-2"><ThemeToggle /><LanguageSwitcher locale={locale} pathAfterLocale={pathAfterLocale} /></div></div></header>;
}
